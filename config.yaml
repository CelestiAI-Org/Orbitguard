data:
  json_path: "data/download.json"

model:
  type: "LSTM"
  sequence_length: 5
  hidden_size: 64
  num_layers: 2

training:
  epochs: 50
  learning_rate: 0.01

output:
  model_path: "models/lstm_model.pth"
  plots_dir: "plots"
