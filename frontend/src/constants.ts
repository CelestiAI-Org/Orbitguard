// Constants now only hold configuration or static UI data, not mock domain data.
// Domain data is sourced exclusively from the Python backend.

export const API_BASE_URL = 'http://localhost:8000';

export const REFRESH_INTERVAL_MS = 60000; // 1 minute
